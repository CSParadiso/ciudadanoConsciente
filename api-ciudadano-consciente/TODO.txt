// TODO Generar Votos
// TODO Generar Preguntas

// TODO Asignar Rol a Usuario en Organización(TABLA users_roles_organizations) en ORGANIZACION
// TODO Asignar Rol a Usuario en Organización(TABLA users_roles_organizations) en USUARIO
// TODO Asignar Rol a Usuario en Organización(TABLA users_roles_organizations) en ROL

// TODO Generar Respuestas
// TODO Generar Actividades


YA SE PUEDE
// TODO Asignar Rol a Usuario en Nivel(TABLA users_roles_levels) en USUARIO
// TODO Asignar Rol a Usuario en Nivel(TABLA users_roles_levels) en ROL

/


IMPLEMENTACIONES
// FIXME Considerar responder con la clase Java WebResponseException en lugar de las excepciones de Quarkus
// FIXME Cambiar los 204 por 404 cuando no existe el recurso (en el GET)

// DOING
// TODO Generar Tipos de Actividades





// DONE
#0 Pasar argumentos como TransferiblesRequest en el POST y PATCH

- Commit fe565c2
#1 Cambiar los 200 por 201 cuando se crea con éxito el POST y anexar el header location con la url del recurso creado
#2 Eliminar las verificaciones de null en los DELETE (el único campo viene por PATH)
#3 Corroborar que las referencias sean consistentes (Nivel no sea padre de sí mismo en NIVEL)
#4 Corroborar que las referencias sean consistentes (Unicidad de title en el mismo nivel en REFERENCIA)
#5 Asignar Rol a Usuario en Nivel(TABLA users_roles_levels) en NIVEL

- Commit 639bb55
#6 Generar Tipos de Actividades (POST y GETall)
#7 Unir URL del Tipo de Actividad (template y parameterModel viven en la misma carpeta remota- Hablado con Fede.)

- Commit
#8 Añadida Redirección a FrontEnd (localhost:8080/app lleva a localhost:5173)