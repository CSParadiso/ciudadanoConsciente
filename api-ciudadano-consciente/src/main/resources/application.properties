# Loggeo
quarkus.log.category."ciudadano.consciente".level=DEBUG

# PASAR AL COMPOSE ESTAS DOS PROPIEDADES
# https://quarkus.io/guides/all-config#quarkus-hibernate-orm_quarkus-hibernate-orm-mapping-timezone-default-storage
# Definir timezone ya que no lo persistimos en DB pero podemos formatear la fecha (al escribir)
quarkus.hibernate-orm.jdbc.timezone=America/Argentina/Ushuaia
# Al leerlo lo normalizamos a la zona horaria de la propiedad (al mapear, leer)
quarkus.hibernate-orm.mapping.timezone.default-storage=normalize

# Persistir loggeo en archivo
quarkus.log.file.enable=true
quarkus.log.file.path=logs/${quarkus.application.name}
#quarkus.log.file.format=%d{yyyy-MM-dd HH:mm:ss,SSS\ %h %N[%i] %-5p [%c{3.\}] (%t) %s%e%n}
quarkus.log.file.rotation.file-suffix=.yyyyMMdd
quarkus.log.file.rotation.rotate-on-boot=true

#cors
quarkus.http.cors=true
quarkus.http.cors.origins=*

# Seleccionamos otro puerto que no sea el por defecto
quarkus.http.port=5909
#quarkus.http.host=0.0.0.0

# VARIABLES QUE SE PUEDEN DEFINIR DESDE EL COMPOSE
    # Acceder al fileSystem
    # Thumbnail images
file.system.images.version.thumbnail=/home/saimon/Projects/CiudadanoConsciente/resources/images/activity-type-version/thumbnail
    # Content images
file.system.images.content=/home/saimon/Projects/CiudadanoConsciente/resources/images/content

# Setear el máximo file size de una imagen
image.max.mb.size=5

# Solo necesario si hay más BD conectadas
quarkus.datasource.db-kind=postgresql

# Determinar user, contraseña, url y conexiones

# Container (3707:5432 --> postgresql 16)
#quarkus.datasource.username=saimon
#quarkus.datasource.password=chipaParaguaya
#quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:3707/ciudadano_consciente

# Container (3700:5432 --> postgresql 13.14-alpine)
quarkus.datasource.username=saimon
quarkus.datasource.password=chipaParaguaya
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:3700/ciudadano_consciente

# Local
# quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/ciudadano_consciente
quarkus.datasource.jdbc.max-size=16

# hibernate
quarkus.hibernate-orm.database.generation=none
#quarkus.hibernate-orm.log.sql=true

# Remover el banner de Quarkus
quarkus.banner.enabled=false

# keycloak
quarkus.oidc.auth-server-url=http://localhost:3704/realms/Ciudadano
quarkus.oidc.client-id=desa-api-ciudadano-consciente
quarkus.oidc.credentials.secret=kEZxxuVuLh4Lp5G3IdxLd3Tn0QT7X2yf
quarkus.oidc.authentication.user-info-required=true

# front externo
#quarkus.http.cors.origins=http://localhost:5273

# APIs GITHUB
quarkus.rest-client.api-github-content.url=https://raw.githubusercontent.com/

# Para visualizar logs de la api consumida
quarkus.rest-client.logging.scope=request-response
quarkus.rest-client.logging.body-limit=500
quarkus.log.category."org.jboss.resteasy.reactive.client.logging".level=DEBUG